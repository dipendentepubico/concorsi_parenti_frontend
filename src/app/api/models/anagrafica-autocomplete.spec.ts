import { AnagraficaAutocomplete } from './anagrafica-autocomplete';

describe('AnagraficaAutocomplete', () => {
  it('should create an instance', () => {
    expect(new AnagraficaAutocomplete()).toBeTruthy();
  });
});
