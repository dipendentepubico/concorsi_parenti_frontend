import { SortedSearchRequest } from './sorted-search-request';

describe('SortedSearchRequest', () => {
  it('should create an instance', () => {
    expect(new SortedSearchRequest()).toBeTruthy();
  });
});
