import { PagedSortedSearchRequest } from './paged-sorted-search-request';

describe('PagedRequest', () => {
  it('should create an instance', () => {
    expect(new PagedSortedSearchRequest()).toBeTruthy();
  });
});
