import { AlignStatusMessage } from './align-status-message';

describe('AlignStatusMessage', () => {
  it('should create an instance', () => {
    expect(new AlignStatusMessage()).toBeTruthy();
  });
});
